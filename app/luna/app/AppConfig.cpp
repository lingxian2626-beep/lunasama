/*

AppConfig (QSettings wrapper)

Keys:

ui/pos (window position), ui/mode, backend/type (“http”/“proc”),

backend/http_base_url (e.g., http://127.0.0.1:9880).

Public:

QPoint lastPos(), void setLastPos(QPoint),

QString mode(), void setMode(QString), etc.

*/